node_modules
dist
.DS_Store
.vscode
.idea
*.log

# Lockfile intentionally NOT committed — Dockerfile runs fresh npm install
# to get correct Linux native bindings (see Coolify deploy rules)
package-lock.json
yarn.lock
pnpm-lock.yaml

# Designer reference materials kept at repo root (not actual app assets —
# real assets live in public/). Use kebab-case names in public/ when committing.
/Logo*.png
/Logobianco.png
/brandkit.png
/esagonisfondo.png
